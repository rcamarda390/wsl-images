# /etc/wsl.conf — add to (or create) this file in your WSL2 distro
# Enables systemd so that agentmemory.service starts automatically at WSL boot.
#
# To apply: restart the WSL2 distro after editing:
#   wsl --shutdown
#   wsl

[boot]
systemd=true
